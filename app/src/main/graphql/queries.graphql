query listRecipes {
  listRecipes {
    items {
      id
      title
      sectionTitle
      thumbnailURL
    }
  }
}

query getRecipeDetails($id: String!) {
  getRecipeDetails(id: $id) {
    id
    title
    description
    imageURL
    ingredients
    steps
  }
}