query listRecipes {
  listRecipes {
    items {
      id
      title
      sectionTitle
    }
  }
}

query getRecipeDetails($id: String!) {
  getRecipeDetails(id: $id) {
    id
    title
    description
    ingredients
    steps
  }
}